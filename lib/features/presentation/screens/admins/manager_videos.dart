import 'package:flutter/material.dart';
class ManagerVideos extends StatelessWidget {
  const ManagerVideos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Manager Videos'),
      ),
      body: Center(
        child: Text('Manager Videos'),
      ),
    );

  }
}